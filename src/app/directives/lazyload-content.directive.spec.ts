import { LazyloadContentDirective } from './lazyload-content.directive';

describe('LazyloadContentDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyloadContentDirective();
    expect(directive).toBeTruthy();
  });
});
